dev.readfaster.app {
	gzip
	proxy / localhost:8000
	tls cert@preet.am
}

prod.readfaster.app {
	gzip
	proxy / localhost:8000
	tls cert@preet.am
}

readfaster.app {
	header / -server
	gzip
	proxy / localhost:8000
	tls cert@preet.am
	redir {
		if {>from-cloudfront} not true
		/ https://www.readfaster.app{uri}
	}
}
